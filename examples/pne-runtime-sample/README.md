# P.N.E. 再生ランタイムサンプル

完成版 `.pne` の読み込みと、音声・SE・画像表示を確認するためのサンプルです。

`pne-runtime-sample.pne` をTauri版のライブラリ画面にある「完成版.pneを開く」から選択してください。

サンプルの元データを変更して再パッキングする場合は、プロジェクトルート（`po-taru`）で次を実行します。

```text
npm run pne -- pack examples/pne-runtime-sample --runtime
```
